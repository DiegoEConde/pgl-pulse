export interface Producto {
    id: number;
    nombre: string;
    marca: string;
    categoria: string;
}

export interface ProductoFormData {
    nombre: string;
    marca: string;
    categoria: string;
}